{
  "schemaVersion": 1,
  "id": "${id}",
  "name": "${name}",
  "version": "${version}",
  "authors": ["${authors}"],
  "contributors": ["${contributors}"],
  "contact": {
    "sources": "${sources_url}",
    "issues": "${issues_url}",
    "homepage": "${homepage_url}"
  },
  "custom": {
    "modmenu": {
      "links": {
        "modmenu.discord": "${discord_url}"
      }
    }
  },
  "description": "${description}",
  "icon": "assets/icon.png",
  "license": "${license}",
  "environment": "*",
  "accessWidener": "substrate.accesswidener",
  "entrypoints": {
    "main": [
      "dev.vesper.substrate.platform.fabric.FabricEntrypoint"
    ],
    "client": [
      "dev.vesper.substrate.platform.fabric.FabricClientEntrypoint"
    ]
  },
  "mixins": [
    "substrate.mixins.json"
  ],${dependencies}
}
